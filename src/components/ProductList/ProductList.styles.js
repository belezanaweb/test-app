import styled from 'styled-components/native'
import Button from '../Button'

export const ProductsFlatList = styled.FlatList`
  padding: 0 10px;
`

export const FetchButton = styled(Button)`
  margin: 10px 0;
`
