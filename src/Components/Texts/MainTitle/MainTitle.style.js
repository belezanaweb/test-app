import styled from 'styled-components/native';
import { Layout, Colors } from '../../../Styleguide';

export const MainTitleComp = styled.Text`
  ${Layout.Type.body};
  font-weight: bold;
  text-align: center;
  padding: 10px 0;
`;
