/**
 * I locked on the pr-br because the boticario test does not need more than that.
 * If there was another file, in an internationalisation case, for example,
 * this JSON could easily be consumed from some endpoint.
 * I will just need update a little logic here and enjoy.
 *
 * -> can use i18n instead of this too :D
 */

import appLabels from './pt-BR.json';

export default appLabels;
